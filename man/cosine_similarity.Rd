% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosine.R
\name{cosine_similarity}
\alias{cosine_similarity}
\title{Whole-string cosine similarity from embeddings}
\usage{
cosine_similarity(
  candidate,
  reference,
  host = "http://localhost:8080",
  prefix = "",
  pooling = c("mean", "cls")
)
}
\arguments{
\item{candidate}{A single candidate string.}

\item{reference}{A single reference string.}

\item{host}{Base URL of the llama.cpp server.}

\item{prefix}{Optional task prefix required by some models (e.g. \code{"query: "}
for the E5 family); see \code{\link[=token_embeddings]{token_embeddings()}}. Default \code{""}.}

\item{pooling}{How to pool token vectors into one: \code{"mean"} (default) or
\code{"cls"}.}
}
\value{
A single cosine similarity, between -1 and 1.
}
\description{
Embeds each string into a single vector and returns the cosine similarity
between them. This is the pooled, whole-string counterpart to \code{\link[=bertscore]{bertscore()}}:
where BERTScore matches individual tokens, this collapses each string to one
vector first, which is faster and is the classic sentence-embedding
similarity. Like \code{\link[=bertscore]{bertscore()}} it requires a running \code{llama.cpp} server; see
\code{\link[=token_embeddings]{token_embeddings()}}.
}
\details{
The server runs with \verb{--pooling none} and returns per-token vectors, so the
pooling into a single vector happens here. \code{pooling = "mean"} averages the
token vectors (the pooling used by sentence-transformers models such as
all-MiniLM and the E5 family); \code{pooling = "cls"} takes the leading special
token's vector (the pooling used by BGE models such as bge-m3). Choose the
one your model was trained with.

Not to be confused with the character-frequency \code{cosine} column from
\code{\link[stringdist:stringsim]{stringdist::stringsim()}} reported by \code{\link[=compare_strings]{compare_strings()}}: that compares
character q-gram counts, while this compares meaning.
}
\examples{
\dontrun{
cosine_similarity("how much do you agree", "to what extent do you agree")
# bge-m3 uses CLS pooling:
cosine_similarity("how much do you agree", "to what extent do you agree",
                  pooling = "cls")
}
}
\seealso{
\code{\link[=bertscore]{bertscore()}} for the token-level counterpart and
\code{\link[=compare_strings]{compare_strings()}} to report this next to the other metrics.
}
