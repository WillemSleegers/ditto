% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bertscore.R
\name{bertscore}
\alias{bertscore}
\title{Compute a BERTScore}
\usage{
bertscore(
  candidate,
  reference,
  host = "http://localhost:8080",
  prefix = "",
  baseline = NULL
)
}
\arguments{
\item{candidate}{A single candidate string.}

\item{reference}{A single reference string.}

\item{host}{Base URL of the llama.cpp server.}

\item{prefix}{Optional task prefix required by some models (e.g. \code{"query: "}
for the E5 family). Applied to both strings and stripped before matching;
see \code{\link[=token_embeddings]{token_embeddings()}}. Default \code{""}.}

\item{baseline}{Optional baseline for rescaling, as returned by
\code{\link[=bertscore_baseline]{bertscore_baseline()}} (a named \code{precision}/\code{recall}/\code{f1} vector) or a
single number applied to all three. Each score \code{x} is rescaled to
\code{(x - b) / (1 - b)}, which maps the unrelated-text floor to 0 and widens
the usable range. Default \code{NULL} (raw scores).}
}
\value{
A named numeric vector with \code{precision}, \code{recall}, and \code{f1}.
}
\description{
Computes a token-level similarity score between a candidate and a
reference using contextual embeddings. Each candidate token is greedily
matched to its most similar reference token (precision) and each reference
token to its most similar candidate token (recall); the F1 is their
harmonic mean. This is the vanilla formulation, without inverse
document-frequency weighting or baseline rescaling.
}
\details{
Requires a running \code{llama.cpp} server; see \code{\link[=token_embeddings]{token_embeddings()}}.

Precision is the mean, over candidate tokens, of each token's highest
cosine similarity to any reference token; recall is the same with the roles
of candidate and reference reversed; F1 is their harmonic mean. Because the
comparison is between contextual token embeddings, a paraphrase that
preserves meaning scores higher than its word overlap alone would suggest.
}
\examples{
\dontrun{
bertscore("how much do you agree", "to what extent do you agree")
}
}
\references{
Zhang, T., Kishore, V., Wu, F., Weinberger, K. Q., & Artzi, Y. (2020).
BERTScore: Evaluating text generation with BERT. \emph{International Conference
on Learning Representations (ICLR)}.
\url{https://doi.org/10.48550/arXiv.1904.09675}
}
\seealso{
\code{\link[=token_embeddings]{token_embeddings()}} for the embeddings this uses,
\code{\link[=bertscore_baseline]{bertscore_baseline()}} to estimate a \code{baseline}, and \code{\link[=compare_strings]{compare_strings()}}
to report it next to the surface metrics.
}
