% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{llama_server}
\alias{llama_server}
\alias{start_llama_server}
\alias{stop_llama_server}
\alias{llama_server_running}
\alias{find_llama_server}
\title{Manage a local llama.cpp embedding server}
\usage{
start_llama_server(
  model = NULL,
  exe = NULL,
  host = "http://localhost:8080",
  pooling = "none",
  extra_args = character(),
  wait = TRUE,
  timeout = 60
)

stop_llama_server()

llama_server_running(host = "http://localhost:8080")

find_llama_server()
}
\arguments{
\item{model}{Path to a \code{.gguf} embedding model. Prefer a model without a
required task prefix (see \code{\link[=token_embeddings]{token_embeddings()}}).}

\item{exe}{Path to the \code{llama-server} executable. Defaults to \code{"llama-server"},
which must be on the \code{PATH}.}

\item{host}{Base URL the server should listen on, also used as the default
\code{host} by \code{\link[=bertscore]{bertscore()}}. The port is taken from this URL.}

\item{pooling}{Pooling mode passed to \code{--pooling}. Must be \code{"none"} for
token-level embeddings.}

\item{extra_args}{Character vector of additional \code{llama-server} arguments.}

\item{wait}{Whether to block until the server answers its health check
(\code{TRUE}) or return immediately (\code{FALSE}).}

\item{timeout}{Seconds to wait for the server to become ready when
\code{wait = TRUE}.
\code{find_llama_server()} looks for the executable without starting it, checking
the \code{ditto.llama_server} option and \code{DITTO_LLAMA_SERVER} environment variable
first, then the \code{PATH}, then a few common install directories. It only reads
the filesystem and never launches anything.}
}
\value{
\code{start_llama_server()} invisibly returns the \link[processx:process]{processx::process}
handle; \code{stop_llama_server()} returns \code{NULL} invisibly;
\code{llama_server_running()} returns a single logical; \code{find_llama_server()}
returns the path to the executable as a string, or \code{NULL} if none is found.
}
\description{
\code{\link[=bertscore]{bertscore()}} and \code{\link[=token_embeddings]{token_embeddings()}} need a running \code{llama.cpp} server
started with \verb{--embedding --pooling none}. Setting that up by hand is
fiddly, so these helpers launch the server as a background process, wait
until it is ready to answer requests, report its status, and shut it down
again.
}
\details{
\code{start_llama_server()} resolves the server binary and model from its
arguments, then from the \code{ditto.llama_server} / \code{ditto.llama_model} options,
then from the \code{DITTO_LLAMA_SERVER} / \code{DITTO_LLAMA_MODEL} environment
variables. Setting the options in your \code{.Rprofile} lets you call
\code{start_llama_server()} with no arguments:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(
  ditto.llama_server = "C:/Users/me/tools/llama.cpp/llama-server.exe",
  ditto.llama_model  = "C:/Users/me/tools/llama.cpp/models/all-MiniLM-L6-v2-f16.gguf"
)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
start_llama_server(
  model = "models/all-MiniLM-L6-v2-f16.gguf",
  exe = "tools/llama.cpp/llama-server.exe"
)
llama_server_running()
bertscore("how much do you agree", "to what extent do you agree")
stop_llama_server()
}
}
\seealso{
\code{\link[=bertscore]{bertscore()}} and \code{\link[=token_embeddings]{token_embeddings()}}, which query the server.
}
