% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chrf.R
\name{chrf}
\alias{chrf}
\title{Compute a CHRF score}
\usage{
chrf(candidate, reference, n = 6, beta = 2)
}
\arguments{
\item{candidate}{A single candidate string.}

\item{reference}{A single reference string.}

\item{n}{Maximum character n-gram order to consider (default 6).}

\item{beta}{Weight of recall relative to precision (default 2, the
standard CHRF setting).}
}
\value{
A CHRF score between 0 and 1.
}
\description{
Computes a character n-gram F-score for a candidate string against a
reference. Where BLEU counts word n-grams and only precision, CHRF counts
character n-grams and combines precision with recall, which makes it more
sensitive to morphological variation: an inflected form that shares no
words with the reference can still share most of its character n-grams.
}
\details{
Precision and recall are computed separately for every n-gram order from 1
to \code{n} and averaged, then combined into an F-score that weights recall
\code{beta} times as much as precision. \code{n} is capped at the length of the
shorter string, the same treatment \code{\link[=bleu]{bleu()}} uses for short strings.
Character n-grams are extracted from the raw string, so spaces count as
ordinary characters and n-grams do not cross word boundaries on their own.
}
\examples{
chrf("agreeing", "agreed")
}
\references{
Popović, M. (2015). chrF: character n-gram F-score for automatic MT
evaluation. \emph{Proceedings of the Tenth Workshop on Statistical Machine
Translation}, 392-395. \url{https://doi.org/10.18653/v1/W15-3049}
}
\seealso{
\code{\link[=bleu]{bleu()}} for the word n-gram, precision-only counterpart, and
\code{\link[=rouge]{rouge()}} for a recall-oriented word n-gram score.
}
