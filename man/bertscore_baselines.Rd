% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bertscore.R
\name{bertscore_baselines}
\alias{bertscore_baselines}
\title{Estimate a BERTScore baseline per language}
\usage{
bertscore_baselines(
  data = example_sentences,
  host = "http://localhost:8080",
  prefix = "",
  n = 400L,
  seed = NULL,
  language_col = "language",
  text_col = "text"
)
}
\arguments{
\item{data}{A data frame with a language column and a text column. Defaults
to \link{example_sentences}.}

\item{host}{Base URL of the llama.cpp server.}

\item{prefix}{Optional task prefix; see \code{\link[=token_embeddings]{token_embeddings()}}. Default \code{""}.}

\item{n}{Number of random pairs to average over per language. Default \code{400}.}

\item{seed}{Optional integer seed for reproducible pairing, applied to each
language. The global random state is restored afterwards.}

\item{language_col, text_col}{Column names holding the language label and the
text. Default \code{"language"} and \code{"text"}.}
}
\value{
A named list with one baseline per language, each a named numeric
vector as returned by \code{\link[=bertscore_baseline]{bertscore_baseline()}}. Index it by language code to
get a \code{baseline} for \code{\link[=bertscore]{bertscore()}}, e.g. \code{baselines[["nl"]]}.
}
\description{
Convenience wrapper that runs \code{\link[=bertscore_baseline]{bertscore_baseline()}} once for each language
in a labelled corpus, since the baseline is language-specific. Useful for
multilingual models such as bge-m3, where you score text in several languages
and want each rescaled against its own floor.
}
\examples{
\dontrun{
baselines <- bertscore_baselines()
bertscore("in hoeverre bent u het eens", "hoeveel bent u het eens",
          baseline = baselines[["nl"]])
}
}
\seealso{
\code{\link[=bertscore_baseline]{bertscore_baseline()}} for a single baseline and \code{\link[=bertscore]{bertscore()}},
which consumes one.
}
