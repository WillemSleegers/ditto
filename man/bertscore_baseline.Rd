% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bertscore.R
\name{bertscore_baseline}
\alias{bertscore_baseline}
\title{Estimate a BERTScore baseline from unrelated text}
\usage{
bertscore_baseline(
  texts = NULL,
  host = "http://localhost:8080",
  prefix = "",
  n = 400L,
  seed = NULL
)
}
\arguments{
\item{texts}{Character vector of distinct, mutually unrelated sentences.
Defaults to the English rows of \link{example_sentences}. For a multilingual
model, pass text in the language you will score.}

\item{host}{Base URL of the llama.cpp server.}

\item{prefix}{Optional task prefix; see \code{\link[=token_embeddings]{token_embeddings()}}. Must match the
prefix used when scoring. Default \code{""}.}

\item{n}{Number of random pairs to average over. Default \code{400}.}

\item{seed}{Optional integer seed for reproducible pairing. The global random
state is restored afterwards.}
}
\value{
A named numeric vector with the baseline \code{precision}, \code{recall}, and
\code{f1}, suitable to pass as \code{baseline} to \code{\link[=bertscore]{bertscore()}}.
}
\description{
Estimates the BERTScore an embedding model assigns to unrelated text, so
scores can be rescaled to span a useful range (see the \code{baseline} argument
of \code{\link[=bertscore]{bertscore()}}). Some models, such as bge-m3, place even unrelated
sentences at a high cosine similarity; subtracting this floor restores the
separation between matched and unmatched pairs.
}
\details{
The baseline is the mean score over \code{n} random, and therefore unrelated,
pairs drawn from \code{texts}. It is specific to the model \emph{and} the language, so
estimate it from text representative of your use. Each sentence is embedded
once and reused across pairs, so a few hundred pairs are cheap to score.
}
\examples{
\dontrun{
b <- bertscore_baseline()
bertscore("how much do you agree", "to what extent do you agree",
          baseline = b)
}
}
\seealso{
\code{\link[=bertscore]{bertscore()}}, which consumes the result.
}
